# agentic-test-factory
an MCP-based framework that auto-generates Playwright test cases from feature behavior guides, converts them into automation scripts using Page Object Model, and supports overwrite/append modes for regression coverage.
